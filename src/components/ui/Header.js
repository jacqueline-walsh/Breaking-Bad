import logo from '../../img/logo.png'

const Header = () => {
    return (
        <header className='center'>
            <img src={logo} alt="logo for breaking bad cast"/>
        </header>
    )
}

export default Header
